# llm-common-cmm.2 — ToolResult provenance contract

## Goal
Standardize a minimal provenance/evidence envelope for tool outputs so both products can render citations consistently.

## Acceptance Criteria
- A schema exists and is versioned.
- Tool outputs can attach evidence in a consistent way.

