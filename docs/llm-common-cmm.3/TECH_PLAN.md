# llm-common-cmm.3 — Optional stream event schema contract

## Goal
If streaming is supported later, define an optional event schema (SSE/WebSocket) that’s consistent across products.

## MVP Note
If MVP is structured-only, this can remain deferred.

