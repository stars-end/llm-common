# llm-common-cmm.6 — Version tag alignment

## Goal
Keep `llm-common` semver tags, `__version__`, and changelog entries consistent so downstream pinning is reliable.

## Acceptance Criteria
- Release checklist exists and is followed.
- Tags match package version.

