# llm-common-cmm.10 — Dexter ports (core primitives)

Tracks identifying Dexter ideas that should be ported into `llm-common` to reduce duplicated agent-workflow logic across app repos.

## Next Step
Break into small, testable primitives (each releaseable and adoptable independently).

