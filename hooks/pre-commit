#!/usr/bin/env bash
# DX_GITHOOKS_SHIM
set -euo pipefail

ROOT="$(git rev-parse --show-toplevel 2>/dev/null || pwd -P)"
HOOK="$(basename "$0")"
TARGET="$ROOT/.githooks/$HOOK"

if [[ -x "$TARGET" ]]; then
  exec "$TARGET" "$@"
fi

exit 0
